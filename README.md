UNO.ai Initial development 

# to setup this project in local

### clone reop
### npm install
### npm start

### if find version issues run the following command in terminal
export NODE_OPTIONS=--openssl-legacy-provider